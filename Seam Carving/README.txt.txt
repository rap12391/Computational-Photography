Python 2 was used for the implementation of this algorithm, however the code should work on Python 3. To run the script, the user must simply run the code. 

Numpy and the lastest OpenCV libarary was used within the script. 